// Лекция 0 - Типы даннных в языке С++
#include <iostream>

using namespace std;

int main()
{
    // Прежде чем разместить в памяти переменную, необходимо указать ее Типы
    // Типы данных в программировании разделяются на:
    // - Числовые(целые - int, вещественные - float, double)
    // - Символьные
    // - Логические
    
    // int - целое число, размер 4 байта, 
    // short - короткие целые числа, размер 2 байта
    // long - длинные целые числа, размер 4 байта
    
    // float - вещественное число одинарной точности, размер 4 байта
    // double - вещественное число двойной точности, размер 8 байт
    
    // char - символьный тип данных, описывает символы, размер 1 байт
    
    // bool - вещественный тип, описывает логические значения, размер 1 байт
    
    // Разные типы данных используют разное количество памяти
    return 0;
}