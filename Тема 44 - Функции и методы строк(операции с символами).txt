// Тема 44 - Функции и методы строк. Операции с символами

#include <iostream>

using namespace std;

int main()
{
    // isupper(c): проверяет, является ли c заглавной буквой, по умолчанию от "A" до "Z"
    cout << isupper('A') << endl; // Проверять можно только один символ
    // Задача: Дан список из строк. Вывести те строки, которые начинаются с заглавной буквы
    
    // islower(c): проверяет, является ли c буквой нижнего регистра, по умолчанию от 'a' до 'z'
    cout << islower('a') << endl;
    // Задача: Дан список из строкю. Вывести те строки, которые оканчиваются строчной буквой
    
    // isalpha(c): проверяет, является ли c алфавитным символом
    cout << isalpha('q') << endl;
    // Задача: Пользоваетль вводит пароль с клавиатуры. Пароль должен состоять только из букв.
    // Проверить, подходит ли введенный пользователем Пароль
    
    // isdigit(c): проверяет, является ли c цифрой от '0' до '9'
    cout << isdigit('1') << endl;
    // Задача. Пользователь вводит пароль с клавиатуры. Пароль должен включать заглавную букву,
    // строчную букву, и не менее 4-х цифр. Проверить, подходит ли введенный пользователем пароль
    
    // isalnum(c): проверяет, является ли c алфавитно-цифровым символом; аналогично isisalpha(c) || isdigit(c)
    
    //isspace(c): проверяет, является ли c пробелом (' '), символом перевода строки ('\n'), 
    //возвратом каретки ('\r'), перевод страницы ('\f'), горизонтальная ('\t') или вертикальная ('\v') табуляция

    //ispunct(c): проверяет, является ли c символом пунктуации 
    //(один из следующих: _ { } [ ] # ( ) < > % : ; ? * + - / ^ & | ~ ! "=")
    
    //Задача. Дана строка. Убрать из нее спец символы, разбить на слова. Найти самое длинное слово
    
    // tolower(c): переводит символ c в нижний регистр
    // toupper(c): переводит символ c в верхний регистр
    char sumbol = 'a';
    sumbol = toupper(sumbol);
    cout << sumbol;
    // Задача: Привести строку формата "HeLlo, MY, FrIeNd" к нормальному типу
    return 0;
}