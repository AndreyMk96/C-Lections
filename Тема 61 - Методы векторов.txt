// Тема 60 - Векторы

#include <iostream>
#include <vector>

using namespace std;

int main()
{
    
    std::vector<int> v3{1, 2, 3, 4, 5};  // вектор v6 состоит из чисел 1, 2, 3, 4, 5
    
    
    // Сравнение векторов
    cout << (v1 != v2) << endl;
    
    // Методы векторов
    cout << v3.size() << endl; // Размер вектора
    cout << v1.empty() << endl; // Проверка на пустоту
    v3.push_back(100); // Добавление элемента в конец
    v3.pop_back(); // Удаление последнего элемента
    
    return 0;
}

// Задачи
// 1) Задать вектор из 10 элементов. Добавить все четные элементы в конец этого вектора
// 2) Проверить является ли вектор невозрастающей / неубывающей последовательностью