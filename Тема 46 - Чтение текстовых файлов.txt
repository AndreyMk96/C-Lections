// Тема 46 - Чтение текстовых файлов

#include <iostream>
#include <fstream>

using namespace std;

int main()
{
    // Если надо считать всю строку целиком или даже все строки из файла, то лучше использовать встроенную функцию getline(),
    // которая принимает поток для чтения и переменную, в которую надо считать текст:
    std::string line;
    std::string result;

    std::ifstream in("test.txt"); // окрываем файл для чтения
    if (in.is_open())
    {
        while (std::getline(in, line))
        {
            //std::cout << line << std::endl;
            result += line + '\n';
        }
    }
    in.close();     // закрываем файл
    cout << result;

    std::ifstream in2("C:\\Users\\anmar\\Documents\\test2.txt"); // если файл лежит не в директории с проектом
    if (in2.is_open())
    {
        while (std::getline(in2, line))
        {
            //std::cout << line << std::endl;
            result += line + '\n';
        }
    }
    in.close();     // закрываем файл
    cout << result;

    // Задачи:
    // 1) Считать крупное литературное произведение, убрать спецсимволы, найти среднюю длину слова
    // 2) Считать крупное литературное произведение, Найти самое длинное слово
    // 3) Считать крупое литературное произведение, пользователь вводит число N. Вывести все слова, длиннее N
    // 4) Считать крупное литературное произведение. Вывести слова, которые начинаются и заканчиваются одинаковой буквой
}