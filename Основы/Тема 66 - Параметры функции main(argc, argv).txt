// Тема N - параметры функции main (argc, argv)

#include <iostream>

using namespace std;

int main(int argc, char * argv[])
{
    // Аргументы функции main нужны, чтобы передать программе параметры в момент запуска (через командную строку)
    // argc - arguments count тип int
    // argv - arguments values массив с указателем на char
    
    // Вывести параметры передаваемые в программу можно таким циклом
    for(int i = 0; i < argc; i++){
        cout << argv[i] << endl;
    }
    // Вывеодится путь к exe файлу, который мы запустили(скомпилированному файлу программы, который мы написали)
    // Это добавляется в argv по умолчанию, если argc и argv указаны в main
    
    // Для передачи в командную строку аргументов необходимо передать путь к exe файлу, и через пробел передать доп Аргументы
    
    // Можно реализовать различное поведение программы, в зависимости от переданных параметров
    
    // Задача: В программе дан массив чисел. Вызвать ее с параметрами -even, или not_even. 
    //В зависимости от параметра программа считает сумму четных или нечетных элементов массива
    return 0;
}